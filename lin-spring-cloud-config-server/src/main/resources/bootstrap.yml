spring:
  application:
    name: config-server 
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/langyan/lin-spring-cloud}
          searchPaths: lin-spring-cloud-config-repo
    
server:
  port: 8888